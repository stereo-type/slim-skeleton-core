import "../css/app.scss";
import 'bootstrap';

// Если у вас не установлен пакет @types/bootstrap,
// вы можете попробовать добавить следующую строку:
// declare var require: any;
// Это позволит TypeScript обойти проверку типов для require.

// Если пакет @types/bootstrap установлен,
// вы можете использовать import вместо require:
// import 'bootstrap';
